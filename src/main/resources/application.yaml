server:
  port: 8080
  address: 0.0.0.0

spring:
  #  password: kzlapgltzlkceade，2251695339@qq.com，dnamrcnrghxdecei
  #  ENjfmB9S6CJSagDL# 授权码，非登录密码
  application:
    name: intellectual-property

  # 默认使用本地库（方便测试）。切回远程时：--spring.profiles.active=remote
  # 或启动时加环境变量 SPRING_PROFILES_ACTIVE=remote
  profiles:
    active: remote

  datasource:
    # 默认占位，会被 profile 覆盖
    url: "jdbc:mysql://127.0.0.1:3306/intellectual-property?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&charset=utf8mb4"
    username: root
    password: xiaojiucheng1226
    driver-class-name: com.mysql.cj.jdbc.Driver
  api-docs:
    enabled: true
    path: /v3/api-docs
    swagger-ui:
      enabled: true
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000ms
      jedis:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 3000ms

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

app:
  upload:
    dir: uploads

logging:
  level:
    org.springframework.transaction: DEBUG
    org.springframework.jdbc.datasource: DEBUG
